Goal: <what>
Constraints:
- React hooks only, TS strict, Tailwind only, lucide-react for icons/logos
- No new packages
Quality:
- Apply KISS first, DRY (rule of 3), SOLID where it reduces coupling
- Use design patterns only if they reduce complexity
Verification:
- Provide minimal diff and a next-check command (build/test)
